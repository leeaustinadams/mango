(ns mango.config
  (:require [environ.core :refer [env]]))

(def db-name (or (:db-name env) "4d4ms-mean"))
(def db-article-collection "blogarticles")
(def db-media-collection "blogmedia")
(def db-users-collection "users")
(def db-sessions-collection "sessions")
(def db-log-collection "logevents")
(def site-title (or (:site-title env) "Lee Austin Adams"))
(def site-description (or (:site-description env) "Lee's Website"))
(def twitter-site-handle (or (:twitter-site-handle env) "@beamjack"))
(def twitter-creator-handle (or (:twitter-creator-handle env) "@beamjack"))
(def admin-email (or (:admin-email env) "admin@4d4ms.com"))
(def version (:mango-version env))
(def port (or (:port env) 8080))
(def aws-credentials {:access-key (:aws-access-key env)
                      :secret-key (:aws-secret-key env)
                      :endpoint (:aws-endpoint env)})
(def aws-media-bucket (or (:aws-media-bucket env) "4d4ms"))
